<%= render('layouts/errors', :object => @comment) %>

<%= form_for [@post, @comment] do |f| %>

  <%= f.label :reply %>
  <%= f.text_field :reply, size: "100x300" %>
  <p><%= f.submit %></p>
<% end %>
